version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com
      - aws eks --region $REGION update-kubeconfig --name $CLUSTER
  build:
    commands:
      - echo Build started on `date`
      - echo Building the Docker image...          
      - docker build -t $IMAGE_REPO_NAME:$IMAGE_TAG .
      - docker tag $IMAGE_REPO_NAME:$IMAGE_TAG $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG      
  post_build:
    commands:
      - echo Build completed on `date`
      - echo Pushing the Docker image...
      - docker push $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG
      - cat "./k8s/app-deployment.yml" | sed "s/{{APPLICATION_NAME}}/$APPLICATION_NAME/g" | sed "s/{{TENANT_ID}}/$TENANT_ID/g" | sed "s/{{AWS_ACCOUNT_ID}}/$AWS_ACCOUNT_ID/g" | sed "s/{{AWS_DEFAULT_REGION}}/$AWS_DEFAULT_REGION/g" | sed "s/{{IMAGE_REPO_NAME}}/$IMAGE_REPO_NAME/g" | sed "s/{{IMAGE_TAG}}/$IMAGE_TAG/g" | kubectl appluy -f - 
